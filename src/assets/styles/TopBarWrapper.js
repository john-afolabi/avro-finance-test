import styled from "styled-components";

const TopBarWrapper = styled.nav`
	form > span {
		position: absolute;
		margin-left: 15px;
		height: 25px;
		display: flex;
		align-items: center;
	}
	input {
		padding-left: 40px;
	}
`;

export default TopBarWrapper;
